---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "wayscloud_dns_zone Resource - terraform-provider-wayscloud"
subcategory: ""
description: |-
  Manages a DNS zone in WAYSCloud.
  A DNS zone represents a domain and contains DNS records. WAYSCloud provides authoritative DNS hosting with automatic replication to multiple nameservers.
  Example Usage
  
  resource "wayscloud_dns_zone" "example" {
    name = "example.com"
  }
  
  Import
  DNS zones can be imported using the zone name:
  
  terraform import wayscloud_dns_zone.example example.com
---

# wayscloud_dns_zone (Resource)

Manages a DNS zone in WAYSCloud.

A DNS zone represents a domain and contains DNS records. WAYSCloud provides authoritative DNS hosting with automatic replication to multiple nameservers.

## Example Usage

```hcl
resource "wayscloud_dns_zone" "example" {
  name = "example.com"
}
```

## Import

DNS zones can be imported using the zone name:

```bash
terraform import wayscloud_dns_zone.example example.com
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The domain name for the DNS zone (e.g., `example.com`). Must be a valid FQDN.

### Optional

- `zone_type` (String) Zone type: `master` (default), `slave`, or `parked`.

### Read-Only

- `created_at` (String) Timestamp when the zone was created (ISO 8601).
- `dnssec_enabled` (Boolean) Whether DNSSEC is enabled for this zone.
- `id` (String) Unique identifier for the DNS zone (UUID).
- `nameservers` (List of String) Authoritative nameservers for this zone. Configure these at your domain registrar.
- `record_count` (Number) Number of DNS records in this zone.
- `status` (String) Zone status: `active`, `suspended`, or `pending_deletion`.
