---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "wayscloud_app Resource - terraform-provider-wayscloud"
subcategory: ""
description: |-
  Manages a container app in WAYSCloud App Platform.
  WAYSCloud App Platform provides fully managed container hosting with automatic
  scaling, TLS, and zero-downtime deployments.
  Example Usage
  Basic App
  
  resource "wayscloud_app" "api" {
    name   = "My API"
    region = "no"
    plan   = "app-basic"
  
    env_vars = {
      NODE_ENV     = "production"
      DATABASE_URL = wayscloud_database.app.connection_string
    }
  }
  
  output "app_url" {
    value = wayscloud_app.api.default_url
  }
  
  App with Scale-to-Zero
  
  resource "wayscloud_app" "worker" {
    name   = "Background Worker"
    region = "no"
    plan   = "app-basic"
  
    port              = 3000
    health_check_path = "/healthz"
  
    scale_to_zero_enabled = true
    idle_timeout_minutes  = 15
    min_instances         = 0
    max_instances         = 3
  }
  
  Deployment
  After creating an app, use the WAYSCloud CLI or API to deploy:
  
  # Deploy from Docker Hub
  curl -X POST "https://api.wayscloud.services/v1/apps/${app_id}/deploy/image" \
    -H "X-API-Key: $WAYSCLOUD_API_KEY" \
    -H "Content-Type: application/json" \
    -d '{"image_uri": "nginx:latest"}'
  
  Import
  Apps can be imported using the app ID:
  
  terraform import wayscloud_app.api app_01ARZ3NDEKTSV4RRFFQ69G5FAV
---

# wayscloud_app (Resource)

Manages a container app in WAYSCloud App Platform.

WAYSCloud App Platform provides fully managed container hosting with automatic
scaling, TLS, and zero-downtime deployments.

## Example Usage

### Basic App

```hcl
resource "wayscloud_app" "api" {
  name   = "My API"
  region = "no"
  plan   = "app-basic"

  env_vars = {
    NODE_ENV     = "production"
    DATABASE_URL = wayscloud_database.app.connection_string
  }
}

output "app_url" {
  value = wayscloud_app.api.default_url
}
```

### App with Scale-to-Zero

```hcl
resource "wayscloud_app" "worker" {
  name   = "Background Worker"
  region = "no"
  plan   = "app-basic"

  port              = 3000
  health_check_path = "/healthz"

  scale_to_zero_enabled = true
  idle_timeout_minutes  = 15
  min_instances         = 0
  max_instances         = 3
}
```

## Deployment

After creating an app, use the WAYSCloud CLI or API to deploy:

```bash
# Deploy from Docker Hub
curl -X POST "https://api.wayscloud.services/v1/apps/${app_id}/deploy/image" \
  -H "X-API-Key: $WAYSCLOUD_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"image_uri": "nginx:latest"}'
```

## Import

Apps can be imported using the app ID:

```bash
terraform import wayscloud_app.api app_01ARZ3NDEKTSV4RRFFQ69G5FAV
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name for the app (3-100 characters).

### Optional

- `env_vars` (Map of String) Environment variables as key-value pairs.
- `health_check_path` (String) Health check endpoint. Default: `/health`.
- `idle_timeout_minutes` (Number) Minutes of inactivity before scaling to zero (1-60). Default: `15`.
- `max_instances` (Number) Maximum instances (1-10). Default: `1`.
- `min_instances` (Number) Minimum instances. Set to 0 for scale-to-zero. Default: `0`.
- `plan` (String) Plan ID. Default: `app-basic`. Available: `app-basic`, `app-standard`, `app-professional`.
- `port` (Number) Port the app listens on. Default: `8080`.
- `region` (String) Region code. Default: `no` (Norway).
- `scale_to_zero_enabled` (Boolean) Enable scale-to-zero on idle. Default: `false`.
- `slug` (String) URL slug (auto-generated from name if not provided). Must be lowercase, start with a letter.

### Read-Only

- `active_deployment_id` (String) Currently active deployment ID.
- `active_image` (String) Currently deployed container image.
- `active_revision_id` (String) Currently active revision ID.
- `created_at` (String) Timestamp when the app was created (ISO 8601).
- `default_url` (String) Default URL for the app (e.g., `https://myapp.apps.wayscloud.services`).
- `id` (String) Unique identifier for the app (ULID with prefix).
- `is_scaled_to_zero` (Boolean) Whether the app is currently scaled to zero.
- `last_deployed_at` (String) Timestamp of last deployment (ISO 8601).
- `plan_name` (String) Human-readable plan name.
- `short_id` (String) Short identifier for display.
- `status` (String) App status: `creating`, `building`, `deploying`, `running`, `stopped`, `error`.
